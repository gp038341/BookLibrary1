using BookLibrary.Domain.Entities;

namespace BookLibrary.Components.Pages
{
    public partial class SearchBooks
    {
        private List<Book>? books;
        /*
        protected override async Task OnSearchClicked()
        {
            filteredBooks = await Repository.SearchByNameAsync();
        }
        */
    }
}
